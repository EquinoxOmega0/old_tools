program kuben;

uses
  Forms,
  main in 'main.pas' {Form1},
  turtle in 'turtle.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
